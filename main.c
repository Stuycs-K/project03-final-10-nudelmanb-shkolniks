#include "main.h"

/*
This function is used to ssh into a designated the computers in one (or several) StuyCS labs
and download/run an executable file that makes it run like a 
*/
int connect(int lab_number, int machine_number){

}


int main(int argc, char *argv[]){
    connect();
}